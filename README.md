# Stochastic Stick-Slip Vibration Suppression

## Local development

The local baseline uses macOS, Python 3.12, `uv`, and Docker Desktop.

```bash
uv sync
uv run python scripts/check_environment.py
uv run pytest -q
```
